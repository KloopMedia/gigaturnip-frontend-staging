import React from "react";

import {WidgetProps} from "@rjsf/core";

const WebhookWidget = (props: WidgetProps) => {

    return (
        <p>Здесь будет webhook trigger</p>
    );
};

export default WebhookWidget;