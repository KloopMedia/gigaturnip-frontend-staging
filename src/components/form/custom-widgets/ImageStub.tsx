import React from "react";

import {WidgetProps} from "@rjsf/core";

const ImageWidget = (props: WidgetProps) => {

    return (
        <p>Здесь будет image</p>
    );
};

export default ImageWidget;